1，接口中声明的变量默认都是 final 的。抽象类可以包含非 final 的变量。 
   Java 接口中的成员函数默认是 public和抽象 的。抽象类的成员函数可以是 private，protected 或者 是 public。

2，同步方法和同步代码块的区别是什么？ 
    同步块会有一个同步的”目标“，使得同步块更加灵活一些（同步块可以通过”目标“决定需要锁定的对象）。
    一般情况下，如果此”目标“为this，那么同步方法和同步块没有太大的区别。
    通过反编译可以看出，同步块比同步方法多了两个指令。因此同步方法是比同步块要快一些
